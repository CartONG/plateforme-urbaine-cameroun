#COMPOSE_FILE=compose.yaml:compose.prod.yaml:compose.dev.yaml
COMPOSE_FILE=compose.yaml:compose.override.yaml:compose.dev.yaml

# set as 'http' because uat and dev under a caddy reverse proxy which handles https
CADDY_PROTOCOL=http
#MAILER_DSN=smtp://cartong-org.mail.protection.outlook.com:25
MAILER_DSN=smtp://mailpit:1025
GOAT_COUNTER_NAMESPACE=puc-test