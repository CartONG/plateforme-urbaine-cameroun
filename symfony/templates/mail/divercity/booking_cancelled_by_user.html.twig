{# booking_cancelled_by_user.html.twig #}
{{ 'mail.divercity.booking_cancelled_by_user.content'|trans({
    '%space_name%': booking.space.name,
    '%date%': booking.date|date('d/m/Y'),
    '%user_fullname%': booking.firstName ~ ' ' ~ booking.lastName,
})|raw }}